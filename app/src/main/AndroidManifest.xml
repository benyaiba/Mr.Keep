<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="net.yaiba.keep" >
    <uses-sdk />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS"
        tools:ignore="ProtectedPermissions" />
    <application
        android:name="net.yaiba.keep.data.ListViewData"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        
        <activity
            android:name="net.yaiba.keep.LoginActivity"
            android:label="Mr.Keep 密码仓库" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="net.yaiba.keep.AddRecordActivity"  android:label="@string/add_a_record" ></activity>
        <activity android:name="net.yaiba.keep.RecordEditActivity" android:label="@string/edit_a_record" ></activity>
        <activity android:name="net.yaiba.keep.MainActivity"  android:label="Mr.Keep 密码仓库" ></activity>
        <activity android:name="net.yaiba.keep.RecordDetailActivity" android:label="@string/detail_a_record" ></activity>
        <activity android:name="net.yaiba.keep.DataManagementActivity" android:label="@string/data_management" ></activity>
        <activity android:name="net.yaiba.keep.LoginPEditActivity" android:label="@string/login_p_change" ></activity>
    </application>

</manifest>
